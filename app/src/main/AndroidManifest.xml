<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myinfoportal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/wgg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/wgg_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyInfoportal">
        <activity android:name=".WebView" android:screenOrientation="landscape"></activity>
        <activity android:name=".MainActivity"  android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
<uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.GET_ACCOUNTS"
        android:maxSdkVersion="22"
        />
</manifest>